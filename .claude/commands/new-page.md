Create a new Next.js App Router page at `app/$ARGUMENTS/page.tsx`.

Follow these rules:
- Use TypeScript with proper types
- Use Tailwind CSS for styling (responsive layout required)
- Add `"use client"` only if the page needs client-side interactivity
- Import from `@/components/ui/*` for UI primitives (shadcn/ui)
- Keep the component name PascalCase matching the route segment
- Add Korean comments where logic isn't self-evident
- Do NOT create a layout.tsx unless explicitly asked

After creating the file, show the full file path and a brief summary of what was created.
